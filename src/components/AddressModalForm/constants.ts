export enum FormTitles {
  SHIPPING = 'Update shipping address',
  BILLING = 'New billing address',
  NEW = 'Update address',
}

export enum ControlLabels {
  SET_DEFAULT = 'Set as default address',
  ALSO_BILLING = 'Also use as billing address',
  ALSO_SHIPPING = 'Also use as shipping address',
}
